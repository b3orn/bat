---
name: Build bat
on:
  workflow_dispatch:
  push:
    branches:
      - master
permissions:
  contents: read
  id-token: write
jobs:
  build:
    name: Build
    strategy:
      matrix:
        include:
          - name: macos-amd64
            os: macos-latest
            arch: x64
          - name: linux-amd64
            os: ubuntu-latest
            arch: x64
    runs-on: "${{ matrix.os }}"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: "Build"
        run: make RELEASE=1
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: "${{ matrix.name }}"
          path: |
            build
            !build/*/*.o
